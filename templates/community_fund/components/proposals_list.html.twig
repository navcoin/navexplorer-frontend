<div class="row">
    <div class="col-md-12">
        <table class="table">
            <thead>
            <tr>
                <th>Hash</th>
                <th>Duration</th>
                <th class="text-right">Nav Requested</th>
                <th class="text-right">Nav Paid</th>
            </tr>
            </thead>
            <tbody>
            {% for idx, proposal in proposals %}
                <tr class="{% if idx is even %}tr-alt{% endif %}">
                    <td colspan="5">
                        <div class="inline-proposal-description"><a href="{{ path('app_communityfund_view', {'hash': proposal.hash}) }}">{{ proposal.description|capitalize|default("No Description")}}</a></div>
                    </td>
                </tr>
                <tr class="{% if idx is even %}tr-alt {% endif %}tr-sub">
                    <td>{{ proposal.hash }}</td>
                    <td>{{ proposal.duration }}</td>
                    <td class="text-right">{{ proposal.requestedAmount|stake_format }}</td>
                    <td class="text-right">{{ (proposal.requestedAmount - proposal.notYetPaid)|stake_format }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">There are no {{ active|proposalState }} proposals</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>